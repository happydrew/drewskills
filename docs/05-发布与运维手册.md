# 发布与运维手册

## 1. 发布流程

```powershell
git checkout main
git pull
python .\scripts\validate_registry.py
git add .
git commit -m "docs: update skills hub"
git push origin main
```

## 2. 运维关注点

- `skills/` 是否持续保持权威
- GitHub 仓库是否可访问

# 发布与运维手册

## 1. 发布流程

```powershell
git checkout main
git pull
python .\scripts\validate_registry.py
python .\scripts\build_public_catalog.py
git add .
git commit -m "docs: refresh catalog"
git push origin main
```

## 2. 运维关注点

- `skills/` 是否持续保持权威
- GitHub 仓库是否可访问
- `public/` 是否与当前技能清单一致

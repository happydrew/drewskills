# 系统架构说明

## 1. 系统定位

本仓库是 Drew Grant 的公开技能源仓库，不依赖私有安装协议。

## 2. 核心设计

- `skills/` 是唯一权威技能源
- `packs/` 负责技能逻辑分组
- `hub.json` 和 `AGENTS.md` 是仓库级入口

## 3. 使用原则

- 一份技能源，多种生态消费
- 优先沿用现有 agent 生态
- 文档和示例不得写死本地路径
- 不提交安装产物目录
